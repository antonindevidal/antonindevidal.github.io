{
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "main -d dist"
  },
  "devDependencies": {
    "vite": "^2.4.2"
  },
  "dependencies": {
    "gh-pages": "^3.2.3"
  }
}
